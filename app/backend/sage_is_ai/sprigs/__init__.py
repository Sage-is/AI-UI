"""Bonsai™ Sprig™ grafting subsystem — Phase 8.0 first-graft walking skeleton.

The Rootstock™ (this app) grafts capabilities at runtime instead of baking them
into the image. This package is the minimum that proves the Graft Union™ seam:
a supervisor that spawns one local embedding Sprig™ as a loopback subprocess and
points the existing OpenAI-compatible dispatch at it.

See ~/.claude/plans/we-got-to-gtm-stateful-teapot.md (Phase 8.0) and the master
Bonsai™ roadmap Decision #19 for scope and what is intentionally deferred.
"""
