fastapi==0.115.7
uvicorn[standard]==0.35.0
typer
pydantic==2.11.7
itsdangerous
# CVE-2026-24486 (HIGH): arbitrary file write — fixed in 0.0.22
# CVE-2026-42561 (HIGH): pre-0.0.27 parser issue — bumped 2026-05-17.
python-multipart==0.0.30

python-socketio==5.16.3
passlib[bcrypt]==1.7.4
cryptography

requests==2.32.4
# CVE-2025-69223 (HIGH): zip bomb via auto_decompress + 8 more CVEs (smuggling, DoS)
aiohttp==3.13.3
async-timeout
aiocache
aiofiles
starlette-compress==1.6.0
beautifulsoup4
# Extras trimmed (8.I.1): zstd/cli/brotli silently re-installed zstandard,
# Brotli, rich, and pygments — keeping any future dep cut from sticking.
# Response decompression stays available via whatever the middleware deps pin.
httpx[socks,http2]==0.28.1

sqlalchemy==2.0.38
alembic==1.14.0
peewee==3.18.1
peewee-migrate==1.12.2
bcrypt==4.3.0
pytz
websocket-client
# huggingface_hub — rides Sprig™ overlays / wizard ml_packages (8.I.2);
# the only base import site (snapshot_download) is lazy.

redis

pycrdt==0.12.25

loguru==0.7.3
asgiref==3.8.1

tiktoken

# RAG engines (langchain + langchain-community + numpy + format-loader deps)
# ride the rag-loaders Sprig™ (8.I.2). Import sites are lazy/guarded:
# routers/retrieval.py (splitters), retrieval/loaders/main.py (loaders),
# retrieval/utils.py (hybrid retrievers), retrieval/web/utils.py (web loaders,
# restart-to-activate). CVE pins move with them into the sprig build:
#   langchain==0.3.30 (CVE-2026-45134), langchain-community==0.3.27
#   (CVE-2025-6984), pypdf==4.3.1, docx2txt==0.8, numpy<2, rank_bm25
# langchain-core STAYS — it is the interface layer (Document, BaseLoader,
# BaseRetriever) subclassed across the codebase. Pinned to the version the
# sprig-side packages expect.
langchain-core==0.3.86

## Vector DB — chromadb is NOT baked into the base rootstock (Bonsai™ slimming).
## Its ~170MB closure (chromadb, onnxruntime, kubernetes, grpc, hnswlib, posthog)
## is delivered by grafting the `vector-chroma` Sprig™ (sha256-pinned OCI artifact
## extracted into site-packages; restart to activate). retrieval/vector/factory.py
## already degrades to VECTOR_DB_CLIENT=None when chromadb is absent at boot.
## Other backends (pymilvus, qdrant-client, opensearch-py, elasticsearch, pinecone)
## still install at runtime via the AI Engine wizard.
# tokenizers / huggingface-hub / numpy left the base (8.I.2) — they ride the
# vector-chroma Sprig™ overlay (repacked to include them) and the rag-loaders
# Sprig™. The wizard ml_packages path still works: with the system copies
# absent, uv resolves them into ml_packages itself. Version constraints that
# the sprig builds must respect (transformers 4.57.6 interplay):
#   - tokenizers<=0.23.0, huggingface-hub<1.0, numpy<2

# ML packages (torch, sentence-transformers, transformers, colbert-ai, faster-whisper,
# onnxruntime) — installed at runtime via the AI Engine wizard step (requirements-ml.txt)


# Document processing: pypdf + docx2txt ride the rag-loaders Sprig™ (8.I.2)
# — they exist only for langchain_community.document_loaders.
# unstructured + python-pptx — installed at runtime when non-PDF document types are uploaded
ftfy==6.2.3
# fpdf2 (+ fontTools + pillow + CJK fonts) rides the export-document Sprig™
# (8.I.2). CVE pins move into the sprig build: fpdf2==2.8.2, pillow==12.2.0
# (CVE-2025-48379, CVE-2026-25990, CVE-2026-40192).
Markdown==3.7
validators==0.35.0
# soundfile — removed (8.I.2): imported nowhere in the codebase (dead dep).

# CVE-2026-32597 (HIGH): accepts unknown crit header extensions — fixed in 2.12.0
PyJWT[crypto]==2.13.0
# CVE-2026-27962 (CRITICAL): auth bypass via JWK header — fixed in 1.6.9
authlib==1.6.9

# black — installed at runtime if code formatting is needed

pydub

# Google Drive / Cloud — installed at runtime when integration is enabled
# google-api-python-client, google-auth-httplib2, google-auth-oauthlib
# googleapis-common-protos, google-cloud-storage

# Azure — installed at runtime when integration is enabled
# azure-identity, azure-storage-blob, azure-ai-documentintelligence

# Test packages (pytest, docker) — install separately for testing, not in production image

## LDAP
ldap3==2.9.1

## Trace
opentelemetry-api==1.32.1
opentelemetry-sdk==1.32.1
opentelemetry-exporter-otlp==1.32.1
opentelemetry-instrumentation==0.53b1
opentelemetry-instrumentation-fastapi==0.53b1
opentelemetry-instrumentation-sqlalchemy==0.53b1
opentelemetry-instrumentation-redis==0.53b1
opentelemetry-instrumentation-requests==0.53b1
opentelemetry-instrumentation-logging==0.53b1
opentelemetry-instrumentation-httpx==0.53b1
opentelemetry-instrumentation-aiohttp-client==0.53b1
