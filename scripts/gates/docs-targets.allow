# Targets a doc PROPOSES but that do not exist yet. One name per line, with a
# reason. An entry here is a recorded intention; an unlisted stray is a lie.
#
# name                     reason
try_sage_smoke             # docs/try-sage-docker-exploration.md PR 6 — proposed CI smoke check
try_sage_stop              # same doc — proposed, never written; the doc now says so
try_sage_build_n_start     # same doc — an option it explicitly recommends AGAINST
caprover_app_create        # TODO.md open item — proposed CapRover wrapper
